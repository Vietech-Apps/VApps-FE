const title = "Company";
const dataRoute = "meta/company/profile";
const navPath = "/admin/company-setting";

export const companyInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  permission: "admin", //user //admin // moduleName
  dataRoute,
  navPath,
  title,
};
