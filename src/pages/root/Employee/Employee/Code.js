const title = "Employee";
const dataRoute = "admin";
const navPath = "/apps/employees/employee";

export const employeInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  permission: "admin", //user //admin // moduleName
  dataRoute,
  navPath,
  title,
};
