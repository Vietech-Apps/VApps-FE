const title = "BinLocation";
const dataRoute = "erp/BinLocation";
const navPath = "/inventory/binlocations";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  navPath,
  title,
  permission: "admin", //user //admin // moduleName
};
