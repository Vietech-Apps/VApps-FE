
const title = "Warehouse";
const dataRoute = "erp/Warehouse";
const navPath = "/inventory/warehouse"

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  navPath,
  title,
  permission: "admin", //user //admin // moduleName
};
