const title = "Receipts";
const dataRoute = "/erp/Inventory/Receipts";
const navPath = "/Inventory/Receipts";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  navPath,
  title,
  permission: "admin", //user //admin // moduleName
};
