import { AppRowContainer } from "meta";
import AppCard from "meta/core/AppCard";
import styled from "styled-components";

export const StyledPackageFourCard = styled(AppCard)`
  align-items: center;
`;
export const StyledPackageFourRow = styled(AppRowContainer)`
  max-width: 1000px;
  justify-content: center;
`;
