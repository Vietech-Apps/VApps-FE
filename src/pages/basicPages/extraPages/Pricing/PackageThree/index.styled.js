import { AppRowContainer } from "meta";
import AppCard from "meta/core/AppCard";
import styled from "styled-components";

export const StyledPackageThreeCard = styled(AppCard)`
  align-items: center;
`;
export const StyledPackageThreeRow = styled(AppRowContainer)`
  max-width: 1000px;
  justify-content: center;
`;
