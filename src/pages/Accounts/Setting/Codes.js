const title = "Settings";
const dataRoute = "/accounts/GeneralSetting";
const navPath = "/settings/accounting";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  permission: "admin", //user //admin // moduleName
  dataRoute,
  navPath,
  title,
};
