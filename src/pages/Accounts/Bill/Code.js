const title = "Bill";
const dataRoute = "/accounts/Bill";
const navPath = "/accounts/vendorBill";

export const billInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  permission: "admin", //user //admin // moduleName
  navPath,
  title,
};
