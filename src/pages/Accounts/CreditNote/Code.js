const title = "Credit Note";
const dataRoute = "/accounts/creditNote";
const navPath = "/accounts/creditNote";

export const creditNoteInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  permission: "admin", //user //admin // moduleName
  navPath,
  title,
};
