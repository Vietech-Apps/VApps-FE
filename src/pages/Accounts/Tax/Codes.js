const title = "Tax";
const dataRoute = "/erp/tax";
const navPath = "/accounts/tax";

export const taxInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  permission: "admin", //user //admin // moduleName
  navPath,
  title,
};
