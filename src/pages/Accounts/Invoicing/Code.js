const title = "Invoicing";
const dataRoute = "/erp/invoice";
const navPath = "/accounts/invoicing";

export const invoiceInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  permission: "admin", //user //admin // moduleName
  navPath,
  title,
};
