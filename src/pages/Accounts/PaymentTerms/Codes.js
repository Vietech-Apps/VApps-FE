const title = "Payment Terms";
const dataRoute = "/accounts/paymentTerms";
const navPath = "/accounts/paymentTerms";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  navPath,
  title,
  permission: "admin", //user //admin // moduleName
};
