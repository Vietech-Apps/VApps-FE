const title = "Contact";
const dataRoute = "meta/contact";
const navPath = "/meta-workspace/contacts";

export const contactInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  permission: "admin", //user //admin // moduleName
  navPath,
  title,
};
