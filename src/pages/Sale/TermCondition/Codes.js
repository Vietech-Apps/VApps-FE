const title = "Term&Conditions";
const dataRoute = "/accounts/Payment";
const navPath = "/sales/term-and-conditions";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  dataRoute,
  navPath,
  title,
  permission: "admin", //user //admin // moduleName
};
