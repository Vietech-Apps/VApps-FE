const title = "Enquiry";
const dataRoute = "erp/enquiry";
const navPath = "/sales/Enquiry";

export const dataInfo = {
  formTitle: title,
  LogTitle: title,
  tableTitle: title,
  metaData: title,
  permission: "admin", //user //admin // moduleName
  dataRoute,
  navPath,
  title,
};
